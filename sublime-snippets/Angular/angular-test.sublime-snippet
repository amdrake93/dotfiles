<snippet>
	<content><![CDATA[
const test = require('tape-catch')
const testApp = require('common/angular-test')
const setup$1 = require('./$2.angular.js')

test('$3', t => {
	testApp(t, app => {
		setup$1(app)
		app.run(($4) => {
			$5
		})
	})
})

]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>testang</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.js</scope>
	<description>angular test</description>
</snippet>
